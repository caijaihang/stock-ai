<Window x:Class="StockAutoTrader.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="StockAutoTrader - 股票自动监控与交易" 
        Height="800" Width="1400"
        WindowStartupLocation="CenterScreen">
    <Window.DataContext>
        <vm:MainViewModel xmlns:vm="clr-namespace:StockAutoTrader.App.ViewModels"/>
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 标题栏 -->
        <Border Grid.Row="0" Background="#FF2D2D2D" Padding="15,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="StockAutoTrader" FontSize="22" FontWeight="Bold" Foreground="White"/>
                    <TextBlock Text="股票自动监控与模拟交易" FontSize="14" Foreground="#FFAAAAAA" Margin="15,0,0,0" VerticalAlignment="Bottom"/>
                </StackPanel>
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="{Binding StatusText}" Foreground="White" FontSize="14" Margin="0,0,20,0" VerticalAlignment="Center"/>
                    <Button Content="启动" Command="{Binding StartCommand}" Width="80" Height="32" Margin="5,0" Background="#FF4CAF50" Foreground="White" BorderThickness="0"/>
                    <Button Content="停止" Command="{Binding StopCommand}" Width="80" Height="32" Margin="5,0" Background="#FFF44336" Foreground="White" BorderThickness="0"/>
                    <Button Content="全部启动" Command="{Binding StartAllCommand}" Width="80" Height="32" Margin="5,0"/>
                    <Button Content="全部停止" Command="{Binding StopAllCommand}" Width="80" Height="32" Margin="5,0"/>
                    <Button Content="一键清仓" Command="{Binding LiquidateCommand}" Width="80" Height="32" Margin="5,0" Background="#FFFF9800" Foreground="White" BorderThickness="0"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- 账户概览 -->
        <Border Grid.Row="1" Background="#FFF5F5F5" Padding="15,10" BorderBrush="#FFDDDDDD" BorderThickness="0,0,0,1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <TextBlock Text="总资产" Foreground="#FF666666" FontSize="12"/>
                    <TextBlock Text="{Binding Account.TotalAssets, StringFormat=C}" FontSize="18" FontWeight="Bold" Foreground="#FF333333"/>
                </StackPanel>
                <StackPanel Grid.Column="1">
                    <TextBlock Text="可用资金" Foreground="#FF666666" FontSize="12"/>
                    <TextBlock Text="{Binding Account.AvailableCash, StringFormat=C}" FontSize="18" FontWeight="Bold" Foreground="#FF333333"/>
                </StackPanel>
                <StackPanel Grid.Column="2">
                    <TextBlock Text="总市值" Foreground="#FF666666" FontSize="12"/>
                    <TextBlock Text="{Binding Account.TotalMarketValue, StringFormat=C}" FontSize="18" FontWeight="Bold" Foreground="#FF333333"/>
                </StackPanel>
                <StackPanel Grid.Column="3">
                    <TextBlock Text="总盈亏" Foreground="#FF666666" FontSize="12"/>
                    <TextBlock Text="{Binding Account.TotalPnl, StringFormat=C}" FontSize="18" FontWeight="Bold" Foreground="{Binding Account.TotalPnl, Converter={StaticResource ProfitLossColorConverter}}"/>
                </StackPanel>
                <StackPanel Grid.Column="4">
                    <TextBlock Text="盈亏率" Foreground="#FF666666" FontSize="12"/>
                    <TextBlock Text="{Binding Account.TotalPnlPercent, StringFormat={}{0:F2}%}" FontSize="18" FontWeight="Bold" Foreground="#FF333333"/>
                </StackPanel>
                <StackPanel Grid.Column="5" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <TextBlock Text="刷新间隔(秒):" VerticalAlignment="Center" Margin="0,0,5,0"/>
                    <ComboBox Width="80" Height="28" SelectedItem="{Binding RefreshIntervalSeconds}">
                        <ComboBoxItem Content="1"/>
                        <ComboBoxItem Content="3"/>
                        <ComboBoxItem Content="5"/>
                    </ComboBox>
                </StackPanel>
            </Grid>
        </Border>

        <!-- 主内容区 -->
        <TabControl Grid.Row="2" Margin="10">
            <TabItem Header="股票监控">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <!-- 添加股票 -->
                    <Border Grid.Row="0" Background="White" Padding="10" Margin="0,0,0,10" CornerRadius="4" BorderBrush="#FFDDDDDD" BorderThickness="1">
                        <StackPanel Orientation="Horizontal">
                            <TextBox Text="{Binding NewStockCode}" Width="100" Height="28" Margin="5,0" ToolTip="股票代码"/>
                            <TextBox Text="{Binding NewStockName}" Width="120" Height="28" Margin="5,0" ToolTip="股票名称"/>
                            <TextBlock Text="买入阈值(%)" VerticalAlignment="Center" Margin="10,0,5,0"/>
                            <TextBox Text="{Binding NewBuyThreshold}" Width="70" Height="28" Margin="5,0"/>
                            <TextBlock Text="卖出阈值(%)" VerticalAlignment="Center" Margin="10,0,5,0"/>
                            <TextBox Text="{Binding NewSellThreshold}" Width="70" Height="28" Margin="5,0"/>
                            <TextBlock Text="买入数量" VerticalAlignment="Center" Margin="10,0,5,0"/>
                            <TextBox Text="{Binding NewBuyQuantity}" Width="70" Height="28" Margin="5,0"/>
                            <TextBlock Text="卖出数量" VerticalAlignment="Center" Margin="10,0,5,0"/>
                            <TextBox Text="{Binding NewSellQuantity}" Width="70" Height="28" Margin="5,0"/>
                            <Button Content="添加" Command="{Binding AddStockCommand}" Width="80" Height="28" Margin="15,0,5,0" Background="#FF2196F3" Foreground="White" BorderThickness="0"/>
                            <Button Content="删除" Command="{Binding DeleteStockCommand}" Width="80" Height="28" Margin="5,0" Background="#FF757575" Foreground="White" BorderThickness="0"/>
                            <Button Content="启停" Command="{Binding ToggleStockCommand}" Width="80" Height="28" Margin="5,0"/>
                        </StackPanel>
                    </Border>

                    <!-- 股票列表 -->
                    <DataGrid Grid.Row="1" ItemsSource="{Binding Stocks}" SelectedItem="{Binding SelectedStock}" 
                              AutoGenerateColumns="False" IsReadOnly="True" GridLinesVisibility="Horizontal"
                              RowHeaderWidth="0" AlternatingRowBackground="#FFF9F9F9">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="代码" Binding="{Binding StockCode}" Width="80"/>
                            <DataGridTextColumn Header="名称" Binding="{Binding StockName}" Width="100"/>
                            <DataGridTextColumn Header="基准类型" Binding="{Binding BenchmarkPriceType}" Width="90"/>
                            <DataGridTextColumn Header="买入阈值(%)" Binding="{Binding BuyThresholdPercent, StringFormat={}{0:F2}}" Width="100"/>
                            <DataGridTextColumn Header="卖出阈值(%)" Binding="{Binding SellThresholdPercent, StringFormat={}{0:F2}}" Width="100"/>
                            <DataGridTextColumn Header="买入数量" Binding="{Binding BuyQuantity}" Width="90"/>
                            <DataGridTextColumn Header="卖出数量" Binding="{Binding SellQuantity}" Width="90"/>
                            <DataGridTextColumn Header="冷却(秒)" Binding="{Binding CooldownSeconds}" Width="90"/>
                            <DataGridTextColumn Header="最大买入" Binding="{Binding MaxBuyTimesPerDay}" Width="80"/>
                            <DataGridTextColumn Header="最大卖出" Binding="{Binding MaxSellTimesPerDay}" Width="80"/>
                            <DataGridTextColumn Header="交易时段" Binding="{Binding TradingHours}" Width="120"/>
                            <DataGridTextColumn Header="T+1" Binding="{Binding T1Enabled}" Width="50"/>
                            <DataGridTextColumn Header="订单类型" Binding="{Binding OrderType}" Width="100"/>
                            <DataGridTextColumn Header="状态" Binding="{Binding Status}" Width="80">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Foreground" Value="{Binding Status, Converter={StaticResource StatusToColorConverter}}"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Header="上次买入触发" Binding="{Binding LastBuyTriggerTime, StringFormat={}{0:HH:mm:ss}}" Width="120"/>
                            <DataGridTextColumn Header="上次卖出触发" Binding="{Binding LastSellTriggerTime, StringFormat={}{0:HH:mm:ss}}" Width="120"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>

            <TabItem Header="持仓">
                <DataGrid ItemsSource="{Binding Positions}" AutoGenerateColumns="False" IsReadOnly="True" GridLinesVisibility="Horizontal">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="代码" Binding="{Binding StockCode}" Width="100"/>
                        <DataGridTextColumn Header="名称" Binding="{Binding StockName}" Width="120"/>
                        <DataGridTextColumn Header="总数量" Binding="{Binding TotalQuantity}" Width="100"/>
                        <DataGridTextColumn Header="可用数量" Binding="{Binding AvailableQuantity}" Width="100"/>
                        <DataGridTextColumn Header="成本价" Binding="{Binding AverageCost, StringFormat=C}" Width="100"/>
                        <DataGridTextColumn Header="现价" Binding="{Binding CurrentPrice, StringFormat=C}" Width="100"/>
                        <DataGridTextColumn Header="市值" Binding="{Binding MarketValue, StringFormat=C}" Width="120"/>
                        <DataGridTextColumn Header="浮动盈亏" Binding="{Binding UnrealizedPnl, StringFormat=C}" Width="120"/>
                        <DataGridTextColumn Header="盈亏率" Binding="{Binding UnrealizedPnlPercent, StringFormat={}{0:F2}%}" Width="100"/>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>

            <TabItem Header="委托">
                <DataGrid ItemsSource="{Binding Orders}" AutoGenerateColumns="False" IsReadOnly="True" GridLinesVisibility="Horizontal">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="委托号" Binding="{Binding OrderId}" Width="180"/>
                        <DataGridTextColumn Header="代码" Binding="{Binding StockCode}" Width="100"/>
                        <DataGridTextColumn Header="方向" Binding="{Binding Side}" Width="80"/>
                        <DataGridTextColumn Header="类型" Binding="{Binding OrderType}" Width="100"/>
                        <DataGridTextColumn Header="数量" Binding="{Binding Quantity}" Width="80"/>
                        <DataGridTextColumn Header="已成交" Binding="{Binding FilledQuantity}" Width="80"/>
                        <DataGridTextColumn Header="委托价" Binding="{Binding Price, StringFormat=C}" Width="100"/>
                        <DataGridTextColumn Header="成交价" Binding="{Binding FilledPrice, StringFormat=C}" Width="100"/>
                        <DataGridTextColumn Header="佣金" Binding="{Binding Commission, StringFormat=C}" Width="100"/>
                        <DataGridTextColumn Header="状态" Binding="{Binding Status}" Width="100"/>
                        <DataGridTextColumn Header="时间" Binding="{Binding OrderTime, StringFormat={}{0:HH:mm:ss}}" Width="100"/>
                        <DataGridTextColumn Header="备注" Binding="{Binding Remark}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>

            <TabItem Header="成交">
                <DataGrid ItemsSource="{Binding Trades}" AutoGenerateColumns="False" IsReadOnly="True" GridLinesVisibility="Horizontal">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="成交号" Binding="{Binding TradeId}" Width="180"/>
                        <DataGridTextColumn Header="代码" Binding="{Binding StockCode}" Width="100"/>
                        <DataGridTextColumn Header="方向" Binding="{Binding Side}" Width="80"/>
                        <DataGridTextColumn Header="数量" Binding="{Binding Quantity}" Width="80"/>
                        <DataGridTextColumn Header="成交价" Binding="{Binding Price, StringFormat=C}" Width="100"/>
                        <DataGridTextColumn Header="成交额" Binding="{Binding Amount, StringFormat=C}" Width="120"/>
                        <DataGridTextColumn Header="佣金" Binding="{Binding Commission, StringFormat=C}" Width="100"/>
                        <DataGridTextColumn Header="时间" Binding="{Binding TradeTime, StringFormat={}{0:HH:mm:ss}}" Width="100"/>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>

            <TabItem Header="日志">
                <DataGrid ItemsSource="{Binding Logs}" AutoGenerateColumns="False" IsReadOnly="True" GridLinesVisibility="Horizontal">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="时间" Binding="{Binding Timestamp, StringFormat={}{0:HH:mm:ss}}" Width="100"/>
                        <DataGridTextColumn Header="级别" Binding="{Binding Level}" Width="80"/>
                        <DataGridTextColumn Header="分类" Binding="{Binding Category}" Width="100"/>
                        <DataGridTextColumn Header="代码" Binding="{Binding StockCode}" Width="100"/>
                        <DataGridTextColumn Header="消息" Binding="{Binding Message}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>
        </TabControl>
    </Grid>
</Window>
